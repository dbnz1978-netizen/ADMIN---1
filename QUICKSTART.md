# Быстрый старт: Система контроля доступа к плагинам

## Что реализовано?

✅ Централизованная система управления доступом к плагинам по ролям  
✅ Страница настроек доступа для плагина "Новости"  
✅ Защита всех страниц плагина новостей  
✅ Автоматическая фильтрация меню в сайдбаре  
✅ HTTP 403 при попытке прямого доступа без прав  
✅ CSRF-защита и логирование  

## Для конечных пользователей

### Как настроить доступ к плагину "Новости"?

1. **Войдите под администратором** (author='admin')

2. **Откройте страницу настроек:**
   - В сайдбаре найдите раздел "Новости"
   - Нажмите на "Настройки"
   - Или перейдите напрямую: `/plugins/news-plugin/pages/settings/access.php`

3. **Настройте права доступа:**
   - Включите/выключите доступ для роли "user"
   - Включите/выключите доступ для роли "admin"
   - Нажмите "Сохранить настройки"

4. **Результат:**
   - Пользователи без прав больше не увидят меню плагина
   - При попытке открыть страницу напрямую получат ошибку 403

### Рекомендуемые настройки

**Вариант 1: Доступ для всех**
- ☑ user: ✓
- ☑ admin: ✓

**Вариант 2: Только администраторы**
- ☐ user: ✗
- ☑ admin: ✓

**Вариант 3: Только пользователи**
- ☑ user: ✓
- ☐ admin: ✗

**Вариант 4: Полный запрет**
- ☐ user: ✗
- ☐ admin: ✗
- ⚠️ Страница настроек всё равно будет доступна admin для изменения

## Для разработчиков

### Как добавить контроль доступа к другому плагину?

#### Шаг 1: Подключить систему в страницах плагина

```php
// В начале каждой PHP-страницы плагина
require_once __DIR__ . '/path/to/admin/functions/plugin_access.php';

// Заменить обычную проверку авторизации на guard
$userDataAdmin = pluginAccessGuard($pdo, 'your-plugin-name');

// Для admin-only страниц
$userDataAdmin = pluginAccessGuard($pdo, 'your-plugin-name', 'admin');
```

#### Шаг 2: Создать страницу настроек

Скопируйте `/plugins/news-plugin/pages/settings/access.php` и измените:

```php
// Изменить имя плагина в guard
$userDataAdmin = pluginAccessGuard($pdo, 'your-plugin-name', 'admin');

// Изменить ключи в настройках
$accessSettings['your-plugin-name'] = [
    'user' => $allowUser,
    'admin' => $allowAdmin
];

// Изменить в UI
$allowUser = $accessSettings['your-plugin-name']['user'] ?? true;
$allowAdmin = $accessSettings['your-plugin-name']['admin'] ?? true;
```

#### Шаг 3: Добавить в nav-config.json

```json
{
  "menu_title": "Your Plugin",
  "menu_icon": "bi-puzzle",
  "submenu": [
    {
      "title": "Настройки",
      "icon": "bi-gear",
      "url": "/plugins/your-plugin/pages/settings/access.php"
    }
  ]
}
```

### API функций

```php
// Проверить доступ
hasPluginAccess($pdo, 'plugin-name', 'user') → bool

// Защитить страницу
pluginAccessGuard($pdo, 'plugin-name') → array
pluginAccessGuard($pdo, 'plugin-name', 'admin') → array

// Получить настройки
getPluginAccessSettings($pdo) → array|false

// Сохранить настройки
savePluginAccessSettings($pdo, $settings) → bool

// Фильтровать меню
filterPluginMenusByAccess($pdo, $menus, $role) → array
```

### Структура данных

```php
// В users.data администратора
[
    'plugins_access' => [
        'news' => [
            'user' => true,
            'admin' => true
        ],
        'another-plugin' => [
            'user' => false,
            'admin' => true
        ]
    ]
]
```

## Безопасность

### Что защищено?

✅ **CSRF:** Все формы с токеном  
✅ **SQL-инъекции:** Prepared statements  
✅ **Прямой доступ:** HTTP 403 для неавторизованных  
✅ **Логирование:** Все события доступа  

### Что НЕ защищено автоматически?

⚠️ **AJAX-запросы:** Нужно добавлять guard в каждый endpoint  
⚠️ **API:** Нужна отдельная реализация  
⚠️ **Другие плагины:** Нужно добавлять guard вручную  

## Расширенные возможности

### Программная проверка доступа

```php
// Показать кнопку только для разрешённых
if (hasPluginAccess($pdo, 'news', $userDataAdmin['author'])) {
    echo '<a href="/plugins/news-plugin/...">Открыть</a>';
}
```

### Условный рендеринг в шаблонах

```php
<?php if (hasPluginAccess($pdo, 'news', $userDataAdmin['author'])): ?>
    <div class="news-section">
        <!-- Контент для пользователей с доступом -->
    </div>
<?php endif; ?>
```

### Кастомные роли (будущее расширение)

Текущая версия поддерживает только `user` и `admin`, но структура готова для добавления новых ролей:

```php
// Будущее API
$accessSettings['news']['moderator'] = true;
pluginAccessGuard($pdo, 'news', 'moderator');
```

## Производительность

- **1 запрос** к БД на загрузку настроек (кешируется в переменной)
- **Нет циклических запросов** при рендеринге меню
- **Минимальное влияние** на скорость загрузки страниц

## Миграция существующих плагинов

### Было:
```php
try {
    $user = requireAuth($pdo);
    if (!$user) {
        header("Location: /admin/logout.php");
        exit;
    }
    $userDataAdmin = getUserData($pdo, $user['id']);
    // ...
} catch (Exception $e) {
    // ...
}
```

### Стало:
```php
require_once __DIR__ . '/path/to/plugin_access.php';
$userDataAdmin = pluginAccessGuard($pdo, 'plugin-name');
// Готово! Всё остальное обрабатывается автоматически
```

## Документация

📄 [PLUGIN_ACCESS_CONTROL_IMPLEMENTATION.md](PLUGIN_ACCESS_CONTROL_IMPLEMENTATION.md) - Полная документация  
📊 [ACCESS_CONTROL_FLOW_DIAGRAMS.md](ACCESS_CONTROL_FLOW_DIAGRAMS.md) - Блок-схемы  
🧪 [TESTING_GUIDE.md](TESTING_GUIDE.md) - Руководство по тестированию  

## Поддержка

При возникновении проблем:

1. Проверьте логи PHP
2. Проверьте логи приложения
3. Убедитесь, что настройки сохранились в БД
4. Проверьте версию PHP (минимум 7.4)

## Чеклист внедрения

- [ ] Протестировать на dev-окружении
- [ ] Проверить все сценарии из TESTING_GUIDE.md
- [ ] Обучить администраторов работе с настройками
- [ ] Настроить логирование (опционально)
- [ ] Задеплоить на production
- [ ] Проверить после деплоя

---

**Версия:** 1.0  
**Дата:** 2026-02-11  
**Статус:** ✅ Готово к использованию

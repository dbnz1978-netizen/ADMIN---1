<?php

/**
 * Название файла:      uninstall.php
 * Назначение:          Скрипт удаления плагина Backup Plugin
 *                      Плагин не создаёт таблицы, поэтому удаление не требуется
 * Автор:               Команда разработки
 * Версия:              1.0
 * Дата создания:       2026-02-12
 * Последнее изменение: 2026-02-12
 */

// ========================================
// ПРОВЕРКА ДОСТУПА
// ========================================

if (!defined('APP_ACCESS')) {
    http_response_code(403);
    exit('Доступ запрещён');
}

/**
 * Функция удаления плагина
 *
 * @param PDO $pdo Объект подключения к базе данных
 * @return array Результат операции ['success' => bool, 'message' => string]
 */
function plugin_uninstall($pdo)
{
    try {
        // Плагин не создаёт таблицы, поэтому удаление таблиц не требуется
        
        return [
            'success' => true,
            'message' => 'Плагин "Система резервного копирования" успешно удален.'
        ];
        
    } catch (Exception $e) {
        return [
            'success' => false,
            'message' => 'Ошибка при удалении плагина: ' . $e->getMessage()
        ];
    }
}
